development: &development
  adapter:  postgresql
  host:     localhost
  encoding: unicode
  database: atomic_lti_development
  pool:     5
  username:
  password:
  template: template0

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test:
  adapter:  postgresql
  encoding: unicode
  database: atomic_lti_test
  pool:     5
  username:
  password:
  template: template0
